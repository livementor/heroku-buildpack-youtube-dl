#!/bin/sh

# intstall youtube-dl
env_dir=$3

mkdir -p foo $env_dir/scripts

curl -L https://yt-dl.org/downloads/latest/youtube-dl -o $env_dir/scripts/youtube-dl
chmod a+rx $env_dir/scripts/youtube-dl
PATH=$PATH:$env_dir/scripts/
