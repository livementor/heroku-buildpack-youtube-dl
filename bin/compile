#!/bin/sh

echo "Installing youtube-dl in "$1"/scripts"

mkdir -p foo $1/scripts

curl -L https://yt-dl.org/downloads/latest/youtube-dl -o $1/scripts/youtube-dl

chmod a+rx $1/scripts/youtube-dl

PATH=$PATH:$1/scripts/
